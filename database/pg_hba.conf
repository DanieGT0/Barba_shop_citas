# Configuración de autenticación PostgreSQL para BarberApp
# ====================================================================
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" es para conexiones Unix domain socket
local   all             postgres                                trust
local   all             all                                     md5

# Conexiones IPv4 locales:
host    all             postgres        127.0.0.1/32            trust
host    all             postgres        ::1/128                 trust

# Permitir conexiones desde Docker containers
host    all             barberapp_user  172.16.0.0/12           md5
host    all             barberapp_user  192.168.0.0/16          md5
host    all             barberapp_user  10.0.0.0/8              md5

# Permitir conexiones desde localhost
host    all             barberapp_user  127.0.0.1/32            md5
host    all             barberapp_user  ::1/128                 md5

# Permitir todas las conexiones con autenticación
host    all             all             0.0.0.0/0               md5
host    all             all             ::/0                    md5

# Conexiones para replicación (si es necesario en el futuro)
# host    replication     postgres        127.0.0.1/32            trust
# host    replication     postgres        ::1/128                 trust